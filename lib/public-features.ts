/**
 * Public launch switches. Keep dormant implementations in the repository so
 * they can be re-enabled deliberately without exposing unfinished surfaces.
 */
export const PUBLIC_FEATURES = {
  slot: false,
} as const;
